.env
.venv/
__pycache__/
*.py[cod]
.DS_Store

# Local runtime and generated artifacts
# Anchor this path at the repository root so the source package
# `gpa/storage` remains trackable.
/storage/
/build/
/dist/
/artifacts/
# The hosted product site has its own Git history and Sites deployment source.
/web/
demo_output/
.omx/
*.egg-info/

# Tool caches and local reports
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
*.log
